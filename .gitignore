# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
.coverage.*

# Secrets and local config
.env
*.pem

# Build artefacts: the index is derived from data/corpus, so it is not source.
data/index/

# Eval output is regenerated by `make eval`. One committed baseline lives in
# evals/baseline/ so regressions are reviewable in a pull request diff.
evals/results/

# Editors and OS
.idea/
.vscode/
.DS_Store
